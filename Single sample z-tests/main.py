quant_saved_mean = st.mean(quant_saved)
quant_saved_median = st.median(quant_saved)
quant_saved_mode = st.mode(quant_saved)

highschool_completed_mean = st.mean(highschool_completed)
highschool_completed_median = st.median(highschool_completed)
highschool_completed_mode = st.mode(highschool_completed)